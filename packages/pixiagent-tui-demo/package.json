{
  "name": "@pixiagent/tui-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint . --ext .ts",
    "start": "bun --silent run src/cli.ts",
    "dev": "bun --silent run src/cli.ts"
  },
  "dependencies": {
    "@opentui/core": "^0.2.7",
    "@pixiagent/core": "workspace:*"
  }
}
